kind: Service
apiVersion: v1
metadata:
  name: {{lower .Release.Name}}-clusterip
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: {{lower .Release.Name}}